<!DOCTYPE html>

<html>
    <head>
        <title>Home</title>
        <link href="css\style1.css" type="text/css" rel="stylesheet"/>
    </head>
    <body>
        <h1>To delete a book, click on the red X button.</h1>
        <div>
        <table id="del">
            <tr>
                <% books.forEach(s=>
                    { %> 
                    <tr>
                        <td>
                            <a id="delete" type="button" value="X" href="<%= `books/${s._id}/delete?_method=DELETE` %>" 
                                onclick="return confirm('Are you sure you want to delete this record?')">X</a>
                        </td>
                        <td>    
                            <p><%= s.book %> </p>
                        </td>
                    </tr>
                    <% });
                %>
            </tr>
        </table>
        </div>
        <%- include('footer') %>
    </body>
</html>